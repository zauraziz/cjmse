/** @type {import('next').NextConfig} */
const nextConfig = {
  // Şriftlər runtime <link> ilə yüklənir; build zamanı xarici şrift fetch-i söndürülür.
  optimizeFonts: false,
};
export default nextConfig;
